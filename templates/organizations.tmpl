{{> mainlessheader.tmpl}}
    <section>
        <ul class="repolist">
        {{#orgs}}
        <li><a href="{{link}}">{{title}}</a></li>
        {{/orgs}}
        </ul>
    </section>
{{> footer.tmpl}}